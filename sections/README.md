# sections

Page-level vertical slices. Each section maps to a distinct area of the landing page and composes components, hooks, and animations.

Sections may import from `components/`, `hooks/`, `lib/`, and `constants/`. They should not be imported by components.
