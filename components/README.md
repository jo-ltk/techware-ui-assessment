# components

Reusable, presentation-focused UI primitives. Each component lives in its own folder with a barrel export.

These are building blocks — not page sections. Sections compose components; components do not import sections.
