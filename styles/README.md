# styles

Global stylesheets for the application. Keeps CSS concerns separate from component logic.

- `globals.css` — Tailwind entry point and global base styles
- `variables.css` — Design tokens (colors, spacing, typography)
- `animations.css` — Shared keyframes and animation utilities
