# hooks

Custom React hooks for shared client-side behavior. Colocated here to keep components declarative and testable.

- `useScrollSpy.ts` — Active section tracking for navigation
- `useIntersection.ts` — Viewport visibility detection
- `useReducedMotion.ts` — Respects `prefers-reduced-motion`
- `useMediaQuery.ts` — Responsive breakpoint matching
