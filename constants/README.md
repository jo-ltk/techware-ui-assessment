# constants

Application-wide static values. Single source of truth for configuration that does not belong in components.

- `colors.ts` — Brand and semantic color tokens
- `navigation.ts` — Nav link definitions
- `site.ts` — Site metadata and global config
- `animations.ts` — Shared animation timing and easing values
